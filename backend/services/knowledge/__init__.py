# ============================================================================
# Knowledge stores: embedding, vector index (Qdrant), graph index (Neo4j),
# and the sync orchestrator that keeps them consistent with MongoDB.
# ============================================================================
