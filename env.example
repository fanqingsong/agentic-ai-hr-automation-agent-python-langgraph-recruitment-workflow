# ============================================================================
# AI HR AUTOMATION - ENVIRONMENT CONFIGURATION
# ============================================================================

# ============================================================================
# LLM PROVIDER CONFIGURATION
# ============================================================================

# Choose your LLM provider: openai, azure, anthropic, gemini, ollama, mock
# - openai: Production (paid, fast, good quality)
# - azure: Production on Azure OpenAI (paid, enterprise-grade)
# - anthropic: Production (paid, excellent reasoning)
# - gemini: Production (paid, multimodal, long context)
# - ollama: Development/Testing (free, local, private)

LLM_PROVIDER=openai
#LLM_PROVIDER=azure
#LLM_PROVIDER=ollama


# ----------------------------------------------------------------------------
# OpenAI Configuration (if LLM_PROVIDER=openai)
# ----------------------------------------------------------------------------
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_KEY=your_openai_api_key

# Available OpenAI models:
# - gpt-4o-mini (recommended, fast, cheap: $0.015/candidate - (~0.17 cents))
# - gpt-4-turbo (best quality, expensive: $0.08/candidate)
# - gpt-4 (legacy, expensive)
# - gpt-3.5-turbo (cheapest, lower quality)


# ----------------------------------------------------------------------------
# Azure OpenAI Configuration (if LLM_PROVIDER=azure)
# ----------------------------------------------------------------------------
AZURE_OPENAI_API_KEY=your_azure_openai_api_key
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=your-deployment-name
AZURE_OPENAI_MODEL=gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Azure OpenAI Setup Instructions:
# 1. Create an Azure OpenAI resource: https://portal.azure.com/
# 2. Deploy a model (e.g., gpt-4o-mini) in Azure OpenAI Studio
# 3. Get the endpoint URL and API key from Azure portal
# 4. Set the deployment name (the name you gave your deployed model)
# 5. Set the model name (e.g., gpt-4o-mini)
# 6. Set the API version (default: 2024-02-15-preview)

# Available Azure OpenAI models:
# - gpt-4o-mini (recommended, fast, cost-effective)
# - gpt-4o (latest, multimodal)
# - gpt-4-turbo (best quality)
# - gpt-35-turbo (cheapest)


# ----------------------------------------------------------------------------
# Anthropic Configuration (if LLM_PROVIDER=anthropic)
# ----------------------------------------------------------------------------
ANTHROPIC_API_KEY=your_anthropic_api_key
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Available Anthropic models:
# - claude-3-opus-20240229 (most capable, $0.12/candidate)
# - claude-3-5-sonnet-20241022 (best balance, $0.025/candidate)
# - claude-3-sonnet-20240229 (good balance, $0.02/candidate)
# - claude-3-haiku-20240307 (fastest/cheapest, $0.012/candidate)

# ----------------------------------------------------------------------------
# Google Gemini Configuration (if LLM_PROVIDER=gemini)
# ----------------------------------------------------------------------------
GEMINI_API_KEY=your_google_gemini_api_key
GEMINI_MODEL=gemini-2.5-pro

# Available Gemini models:
# - gemini-2.5-pro (latest, most capable, multimodal)
# - gemini-1.5-pro (powerful, 2M token context)
# - gemini-1.5-flash (fast, efficient)
# - gemini-pro (standard, good balance)
#
# Get API key: https://makersuite.google.com/app/apikey
# Pricing: https://ai.google.dev/pricing

# ----------------------------------------------------------------------------
# Ollama Configuration (if LLM_PROVIDER=ollama)
# ----------------------------------------------------------------------------
OLLAMA_MODEL=qwen3:0.6b
OLLAMA_BASE_URL=http://localhost:11434

# Available Ollama models (free, local):
# - qwen3:6b (recommended, fast, good quality)
# - llama3 (8B parameters)
# - mistral (7B, fast)

# Setup: ollama pull qwen3:6b

# ----------------------------------------------------------------------------
# LLM Temperature Settings (optional, applies to all providers)
# ----------------------------------------------------------------------------
EXTRACTION_TEMP=0.2
SUMMARY_TEMP=0.5
EVALUATION_TEMP=0.4


# ============================================================================
# LLAMA INDEX & LLAMA CLOUD CONFIGURATION
# ============================================================================

LLAMA_CLOUD_API_KEY=llama_cloud_api_key


# ============================================================================
# GOOGLE CLOUD CONFIGURATION
# ============================================================================

# Copy the downloaded Google Service Account: 'project-id.json' file from google cloud and rename
GOOGLE_CREDENTIALS_JSON_FILE=google-service-account-credentials.json
GOOGLE_SHEET_ID=google_sheet_id

GOOGLE_CLOUD_STORAGE_BUCKET=ai-hr-cv-storage-prod

# How to get these:
#
# GOOGLE_SHEET_ID: Create sheet, get ID from URL
#    https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit
#


# ============================================================================
# MINIO STORAGE CONFIGURATION (对象存储)
# ============================================================================

# MinIO server endpoint
# - Local Docker: localhost:9000
# - Docker Compose: minio:9000 (internal network)
# - Production: your-minio-server.com:9000
MINIO_ENDPOINT=localhost:9000

# MinIO access credentials
# IMPORTANT: Change these in production!
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123

# Bucket name for CV uploads (auto-created on startup)
MINIO_BUCKET=cv-uploads

# Use HTTPS (set to true in production)
MINIO_SECURE=false

# MinIO Console (Web UI)
# URL: http://localhost:9001
# Default credentials: minioadmin / minioadmin123


# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# MongoDB (简历数据和评估结果)
MONGODB_URL=mongodb://localhost:27017

# PostgreSQL (用户认证和权限管理)
POSTGRES_SERVER=localhost:5432
POSTGRES_USER=hr_user
POSTGRES_PASSWORD=hr_pass
POSTGRES_DB=hr_users


# ============================================================================
# SECURITY CONFIGURATION (JWT 认证)
# ============================================================================

# SECRET_KEY for JWT token signing (CHANGE IN PRODUCTION!)
# Generate a secure key with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production-use-openssl-rand-hex-32

# JWT Algorithm
ALGORITHM=HS256

# Access token expiration time (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# FASTAPI CONFIGURATION
# ============================================================================

HOST=0.0.0.0
PORT=8000
RELOAD=false
DEBUG=false
WORKERS=1


# Development settings:
# RELOAD=false
# DEBUG=true
# WORKERS=1


# ============================================================================
# CONFIGURATION EXAMPLES
# ============================================================================

# Example 1: Production with OpenAI (Default)
# --------------------------------------------
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxx
# OPENAI_MODEL=gpt-4o-mini


# Example 2: Development with Ollama (Free!)
# -------------------------------------------
# LLM_PROVIDER=ollama
# OLLAMA_MODEL=qwen3:0.6b
# OLLAMA_BASE_URL=http://localhost:11434
# Cost: $0 (runs locally)
# Setup: ollama pull qwen3:0.6b && ollama serve

